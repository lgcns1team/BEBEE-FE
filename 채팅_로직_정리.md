# 채팅 로직 정리 문서

## 개요

채팅 기능의 전체 흐름과 소켓 통신, 메시지 전송/수신, 매칭확인서 처리 로직을 정리한 문서입니다.

---

## 1. 채팅 로직 결정 사항

### 1.1 소켓 발행 규칙

- **프론트에서 소켓으로 발행하는 것은 텍스트 메시지만 가능**
- 매칭확인서는 소켓으로 발행하지 않음

### 1.2 매칭확인서 생성 흐름

1. 프론트에서 매칭확인서 생성 시 **POST `/match/agreements` API**로 요청
2. 서버에서 `/match`에서 이벤트를 `/chat`으로 발행하고 소켓과 연결
3. 서버에서 매칭확인서, 성공, 실패를 `type`과 `data`로 소켓 발행
4. 프론트에서는 소켓으로 받기만 함

### 1.3 재입장 시 메시지 조회

- 재입장했을 때는 **GET `/chat/chatrooms/chats`**로 조회 가능

---

## 2. 컴포넌트 구조

### 2.1 ChatRoomPage (`src/domain/chat/pages/ChatRoomPage.tsx`)

채팅방 메인 페이지로, 소켓 연결과 메시지 전송/수신을 관리합니다.

#### 주요 기능

1. **채팅방 정보 조회 및 소켓 연결**

   ```typescript
   useEffect(() => {
     // 채팅방 정보 조회
     const roomData = await chatApi.openChatRoom(undefined, chatroomId);
     setActiveRoom(roomData);

     // 소켓 연결
     connect();

     // 언마운트 시 소켓 연결 해제
     return () => disconnect();
   }, [chatroomId]);
   ```

2. **재입장 시 메시지 조회**

   ```typescript
   useEffect(() => {
     // GET /chat/chatrooms/chats로 메시지 내역 조회
     fetchHistory(chatroomId, true);
   }, [chatroomId, activeRoom?.chatroomId]);
   ```

3. **매칭 상태 업데이트**

   ```typescript
   useEffect(() => {
     const messages = getMessages(chatroomId);

     // 최신 메시지에서 MATCH_SUCCESS/MATCH_FAILURE 확인
     const latestMatchMessage = messages
       .reverse()
       .find(
         (msg) => msg.type === "MATCH_SUCCESS" || msg.type === "MATCH_FAILURE"
       );

     if (latestMatchMessage) {
       const newStatus =
         latestMatchMessage.type === "MATCH_SUCCESS"
           ? "MATCHED"
           : "NON_MATCHED";
       updateMatchStatus(newStatus);
     }
   }, [chatroomId, activeRoom?.chatroomId, getMessages(chatroomId).length]);
   ```

4. **텍스트 메시지 전송**
   ```typescript
   const handleSend = useCallback(
     (text: string) => {
       const senderId = Number(user.memberId);
       const receiverId = Number(activeRoom.otherId);

       // 소켓으로 텍스트 메시지 발행
       sendMessage(senderId, receiverId, text, chatroomId);
     },
     [chatroomId, activeRoom, user, sendMessage]
   );
   ```

---

## 3. 소켓 스토어 (useSocketStore)

### 3.1 소켓 연결 (`src/store/useSocketStore.ts`)

#### 연결 설정

- **소켓 URL**: `wss://api.be-bee.link/chat/ws/chats`
- **프로토콜**: STOMP over WebSocket
- **구독 경로**: `/sub/member:{memberId}`
- **발행 경로**: `/pub/chats`

#### 연결 흐름

```typescript
connect: () => {
  const client = new Client({
    brokerURL: SOCKET_URL,
    connectHeaders: {
      "accept-version": "1.2",
      host: window.location.hostname,
      Authorization: `Bearer ${token}`,
    },
    reconnectDelay: 5000,
    heartbeatIncoming: 4000,
    heartbeatOutgoing: 4000,

    onConnect: () => {
      // 구독 설정
      client.subscribe(`/sub/member:${memberId}`, (message) => {
        const receivedMsg: ChatMessage = JSON.parse(message.body);
        const chatroomId = receivedMsg.chatroomId;

        if (chatroomId) {
          // 메시지 추가
          get().addMessage(receivedMsg, chatroomId);
          useChatStore.getState().addMessage(receivedMsg, chatroomId);
        }
      });
    },
  });

  client.activate();
};
```

### 3.2 메시지 전송

#### 텍스트 메시지 전송

```typescript
sendMessage: (senderId, receiverId, text, chatroomId) => {
  const payload = {
    receiverId,
    type: "TEXT",
    textContent: text,
    createdAt: new Date().toISOString(),
    chatroomId,
  };

  // Optimistic Update (UI 즉시 반영)
  const tempMessage: ChatMessage = {
    id: `temp-${Date.now()}`,
    senderId: String(senderId),
    textContent: text,
    type: "TEXT",
    attachments: [],
    createdAt,
    chatroomId,
  };

  get().addMessage(tempMessage, chatroomId);
  useChatStore.getState().addMessage(tempMessage, chatroomId);

  // 소켓 발행
  client.publish({
    destination: "/pub/chats",
    body: JSON.stringify(payload),
    headers: { "content-type": "application/json" },
  });
};
```

**주의**: 매칭확인서는 소켓으로 발행하지 않습니다. `sendMatchConfirmation` 함수는 현재 사용되지 않습니다.

### 3.3 메시지 수신

소켓으로 받은 메시지는 자동으로 `useSocketStore`와 `useChatStore`에 추가됩니다.

#### 메시지 타입

- `TEXT`: 일반 텍스트 메시지
- `IMAGE`: 이미지 메시지
- `MATCH_CONFIRMATION`: 매칭확인서 (서버에서 발행)
- `MATCH_SUCCESS`: 매칭 성공 (서버에서 발행)
- `MATCH_FAILURE`: 매칭 실패 (서버에서 발행)

---

## 4. 채팅 스토어 (useChatStore)

### 4.1 메시지 관리 (`src/domain/chat/store/useChatStore.ts`)

#### 메시지 조회

```typescript
fetchHistory: async (chatroomId: string, isFirstLoad = false) => {
  const cursor = isFirstLoad ? null : roomState.lastChatId;

  const data = await chatApi.getMessages(chatroomId, cursor);

  set((state) => ({
    messagesByChatroom: {
      ...state.messagesByChatroom,
      [chatroomId]: {
        messages: isFirstLoad
          ? data.messages
          : [...data.messages, ...prevRoomData.messages],
        hasNext: data.hasNext,
        lastChatId: data.nextChatId,
      },
    },
  }));
};
```

#### 메시지 추가

```typescript
addMessage: (message, chatroomId) => {
  const targetId = chatroomId || state.activeRoom?.chatroomId;

  // 중복 메시지 방지
  const isDuplicate = currentRoomData.messages.some((m) => m.id === message.id);
  if (isDuplicate) return state;

  // 메시지 추가
  return {
    messagesByChatroom: {
      ...state.messagesByChatroom,
      [targetId]: {
        ...currentRoomData,
        messages: [...currentRoomData.messages, message],
      },
    },
  };
};
```

---

## 5. 전체 흐름도

### 5.1 채팅방 진입 흐름

```
[사용자 채팅방 클릭]
    ↓
[ChatRoomPage 마운트]
    ↓
[1] 채팅방 정보 조회 (POST /chat/chatrooms)
    ├─ activeRoom 설정
    └─ 채팅방 상세 정보 저장
    ↓
[2] 소켓 연결 (useSocketStore.connect)
    ├─ STOMP 클라이언트 생성
    ├─ WebSocket 연결
    └─ /sub/member:{memberId} 구독
    ↓
[3] 메시지 내역 조회 (GET /chat/chatrooms/chats)
    ├─ 첫 로딩 시 전체 메시지 조회
    └─ useChatStore에 저장
    ↓
[채팅방 UI 표시]
```

### 5.2 텍스트 메시지 전송 흐름

```
[사용자 메시지 입력]
    ↓
[ChatInput에서 handleSend 호출]
    ↓
[useSocketStore.sendMessage 호출]
    ↓
[Optimistic Update]
    ├─ 임시 메시지 생성 (temp-{timestamp})
    ├─ useSocketStore에 추가
    └─ useChatStore에 추가
    ↓
[소켓 발행]
    ├─ destination: /pub/chats
    ├─ payload: { type: "TEXT", textContent, ... }
    └─ 서버로 전송
    ↓
[서버 처리]
    ├─ 메시지 저장
    └─ 수신자에게 소켓 발행
    ↓
[실제 메시지 수신]
    ├─ 소켓으로 수신
    ├─ 임시 메시지 교체 또는 추가
    └─ UI 업데이트
```

### 5.3 매칭확인서 생성 흐름

```
[사용자 매칭하기 버튼 클릭]
    ↓
[MatchFormPage로 이동]
    ↓
[매칭확인서 작성]
    ↓
[POST /match/agreements API 호출]
    ├─ 매칭확인서 데이터 전송
    └─ 서버에서 매칭확인서 생성
    ↓
[서버 처리]
    ├─ 매칭확인서 저장
    ├─ /match에서 이벤트 발행
    └─ /chat으로 소켓 발행
        └─ type: "MATCH_CONFIRMATION"
    ↓
[프론트 소켓 수신]
    ├─ useSocketStore에서 수신
    ├─ useChatStore에 추가
    └─ UI에 매칭확인서 카드 표시
```

### 5.4 매칭 결과 수신 흐름

```
[상대방이 매칭확인서 수락/거절]
    ↓
[서버 처리]
    ├─ 매칭 상태 업데이트
    └─ 소켓 발행
        ├─ type: "MATCH_SUCCESS" (수락)
        └─ type: "MATCH_FAILURE" (거절)
    ↓
[프론트 소켓 수신]
    ├─ useSocketStore에서 수신
    ├─ useChatStore에 추가
    └─ ChatRoomPage에서 감지
    ↓
[매칭 상태 업데이트]
    ├─ MATCH_SUCCESS → matchStatus: "MATCHED"
    ├─ MATCH_FAILURE → matchStatus: "NON_MATCHED"
    └─ useChatStore.updateMatchStatus 호출
    ↓
[UI 업데이트]
    ├─ 매칭 성공/실패 카드 표시
    └─ 매칭하기 버튼 상태 변경
```

---

## 6. API 엔드포인트

### 6.1 채팅 관련 API

#### 채팅방 열기/조회

```
POST /chat/chatrooms
Query Params:
  - otherMemberId?: string
  - chatroomId?: string
Body:
  - postId?: string
  - postTitle?: string
  - helpCategoryIds?: number[]
  - isVolunteer?: boolean
Response: ChatroomResponse
```

#### 메시지 조회

```
GET /chat/chatrooms/chats
Query Params:
  - chatroomId: string
  - lastChatId?: string | null
  - count?: number (기본값: 20)
Response: ChatMessagesGetResDTO
  - messages: ChatMessage[]
  - hasNext: boolean
  - nextChatId: string | null
```

### 6.2 매칭 관련 API

#### 매칭확인서 생성

```
POST /match/agreements
Body: AgreementRequest
Response: AgreementResponse
```

**주의**: 매칭확인서는 소켓으로 발행하지 않고, POST API로만 생성합니다.

---

## 7. 메시지 타입 정의

### 7.1 ChatMessage 타입

```typescript
interface ChatMessage {
  id: string;
  senderId: string;
  textContent: string;
  type: MessageType;
  createdAt: string;
  chatroomId: string;
  attachments?: string[];

  // 매칭 관련 필드
  agreementId?: string;
  matchData?: AgreementRequest;
  postId?: string;
  title?: string;
  helperId?: string;
  disabledId?: string;
  isVolunteer?: boolean;
  usedHoney?: number;
  currentHoney?: number;
}
```

### 7.2 MessageType

```typescript
type MessageType =
  | "TEXT" // 일반 텍스트
  | "IMAGE" // 이미지
  | "MATCH_CONFIRMATION" // 매칭확인서 (서버 발행)
  | "MATCH_SUCCESS" // 매칭 성공 (서버 발행)
  | "MATCH_FAILURE"; // 매칭 실패 (서버 발행)
```

---

## 8. 주요 주의사항

### 8.1 소켓 발행 규칙

- ✅ **텍스트 메시지만 소켓으로 발행**
- ❌ **매칭확인서는 소켓으로 발행하지 않음** (POST API 사용)

### 8.2 메시지 중복 방지

- 소켓은 네트워크 상황에 따라 같은 메시지가 두 번 올 수 있음
- 메시지 ID로 중복 체크 후 추가

### 8.3 Optimistic Update

- 텍스트 메시지 전송 시 임시 메시지를 즉시 UI에 표시
- 서버에서 실제 메시지가 오면 임시 메시지 교체 또는 유지

### 8.4 매칭 상태 동기화

- `MATCH_SUCCESS`/`MATCH_FAILURE` 메시지 수신 시 `matchStatus` 자동 업데이트
- `ChatRoomCard`의 매칭하기 버튼 상태가 자동으로 변경됨

---

## 9. 파일 구조

```
src/
├── domain/
│   └── chat/
│       ├── pages/
│       │   └── ChatRoomPage.tsx      # 채팅방 메인 페이지
│       ├── components/
│       │   ├── ChatInput.tsx         # 메시지 입력 컴포넌트
│       │   ├── MessageList.tsx       # 메시지 목록 컴포넌트
│       │   ├── MessageItem.tsx       # 개별 메시지 컴포넌트
│       │   └── ChatRoomCard.tsx      # 채팅방 헤더 카드
│       ├── store/
│       │   └── useChatStore.ts       # 채팅 상태 관리
│       └── types/
│           └── chat.types.ts         # 채팅 관련 타입 정의
├── store/
│   └── useSocketStore.ts              # 소켓 연결 및 메시지 전송 관리
└── api/
    ├── chatApi.ts                     # 채팅 관련 API
    └── matchApi.ts                    # 매칭 관련 API
```

---

## 10. 향후 개선 사항

1. **소켓 연결 안정성**

   - 재연결 로직 개선
   - 연결 상태 UI 표시

2. **메시지 페이징**

   - 무한 스크롤 구현
   - 이전 메시지 로드 최적화

3. **에러 처리**

   - 네트워크 에러 처리
   - 메시지 전송 실패 시 재시도 로직

4. **성능 최적화**
   - 메시지 가상화 (Virtual Scrolling)
   - 이미지 최적화
