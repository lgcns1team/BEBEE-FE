# 매칭확인서 생성 과정 상세 문서

## 개요

매칭확인서 생성은 채팅방에서 "매칭하기" 버튼을 클릭하여 시작되며, `postId`를 활용하여 게시글 정보를 가져온 후 사용자가 수정하여 최종적으로 서버에 매칭확인서를 생성하는 전체 과정을 설명합니다.

---

## 1. 페이지 진입 및 초기화

### 1.1 라우팅
- **경로**: `/chat/:chatroomId/match`
- **컴포넌트**: `MatchFormPage`
- **진입 방법**: 채팅방 페이지(`ChatRoomPage`)에서 "매칭하기" 버튼 클릭

### 1.2 URL 파라미터 추출
```typescript
const { chatroomId } = useParams<{ chatroomId: string }>();
```

---

## 2. 데이터 로딩 과정

### 2.1 채팅방 정보 조회

**목적**: `chatroomId`를 통해 채팅방 정보를 가져와 `postId`를 획득합니다.

**API 호출**:
```typescript
// chatApi.openChatRoom(undefined, chatroomId)
POST /chat/chatrooms
Query Params:
  - chatroomId: string
```

**조건**:
- `activeRoom`이 없거나
- `activeRoom.chatroomId`가 현재 `chatroomId`와 다를 때만 호출

**응답 데이터 구조** (`ChatroomResponse`):
```typescript
{
  chatroomId: string;
  postId: string;  // ← 이 값을 사용하여 게시글 정보 조회
  myId: string;
  otherId: string;
  otherNickname: string;
  // ... 기타 필드
}
```

**스토어 업데이트**:
```typescript
setActiveRoom(roomData);  // useChatStore에 저장
```

### 2.2 게시글 상세 정보 조회

**목적**: `postId`를 활용하여 게시글의 상세 정보를 가져옵니다.

**API 호출**:
```typescript
// postApi.getPostDetail(postId)
GET /match/posts/:postId
```

**응답 데이터 구조** (`PostDetailResponse`):
```typescript
{
  // 회원 정보
  memberNickname: string;
  memberLegalDongCode: string;
  memberProfileImageUrl: string;

  // 게시글 기본 정보
  title: string;
  content: string;
  engagementType: "DAY" | "TERM";
  unitHoney: number;
  totalHoney: number;
  postLegalDongCode: string;

  // 도움 카테고리
  helpCategoryIds: number[];

  // 날짜/시간 정보
  date?: string;              // DAY 타입일 때
  startDate?: string;          // TERM 타입일 때
  endDate?: string;            // TERM 타입일 때
  schedules: Schedule[];       // 스케줄 정보 배열
}
```

**Schedule 구조**:
```typescript
{
  dayOfWeek: "MONDAY" | "TUESDAY" | ...;
  startTime: string;  // "11:00:00"
  endTime: string;   // "14:00:00"
}
```

### 2.3 데이터 변환: PostDetailResponse → MatchPost

**목적**: 서버 응답 데이터를 UI에서 편집 가능한 형태로 변환합니다.

**변환 함수**: `convertPostToMatchPost(post: PostDetailResponse): MatchPost`

#### DAY 타입 변환
```typescript
if (isDay && post.date) {
  const date = new Date(post.date);
  const schedule = post.schedules[0];
  
  return {
    type: "day",
    title: post.title,
    reward: post.unitHoney,
    location: post.postLegalDongCode || "",
    date: date,
    startTime: schedule ? new Date(`${post.date}T${schedule.startTime}`) : null,
    endTime: schedule ? new Date(`${post.date}T${schedule.endTime}`) : null,
  };
}
```

#### TERM 타입 변환
```typescript
if (post.startDate && post.endDate) {
  const weeks = post.schedules.map((schedule) => ({
    day: DAY_OF_WEEK_MAP[schedule.dayOfWeek] || "월",
    start: schedule.startTime 
      ? new Date(`2000-01-01T${schedule.startTime}`) 
      : null,
    end: schedule.endTime 
      ? new Date(`2000-01-01T${schedule.endTime}`) 
      : null,
  }));

  return {
    type: "long",
    title: post.title,
    reward: post.unitHoney,
    location: post.postLegalDongCode || "",
    periodStart: new Date(post.startDate),
    periodEnd: new Date(post.endDate),
    weeks: weeks,
  };
}
```

**상태 저장**:
```typescript
setPostDetail(detail);                    // 원본 게시글 정보
setEditedPost(matchPost);                 // 편집 가능한 형태로 변환된 데이터
setSelectedTags(detail.helpCategoryIds);  // 선택된 도움 카테고리
```

---

## 3. UI 렌더링

### 3.1 로딩 상태
```typescript
if (isLoading || !editedPost) {
  return <LoadingUI />;
}
```

### 3.2 폼 필드 구성

#### 공통 필드
- **도움 유형**: `typeLabelMap[editedPost.type]` (읽기 전용)
- **제목**: `editedPost.title` (읽기 전용)
- **도움 카테고리**: `HelpTagDropDown` 컴포넌트
  - 초기값: `postDetail.helpCategoryIds`
  - 상태: `selectedTags`

#### DAY 타입 전용 필드
- `DayHelpForm` 컴포넌트
  - 날짜 선택
  - 시작 시간
  - 종료 시간

#### TERM 타입 전용 필드
- `LongHelpForm` 컴포넌트
  - 시작 날짜
  - 종료 날짜
  - 주간 스케줄 (요일별 시작/종료 시간)

#### 공통 추가 필드
- **1회 제공 꿀**: `editedPost.reward` (수정 가능)
- **만남 장소**: `LocationInput` 컴포넌트
  - 초기값: `editedPost.location`
  - 카카오 장소 검색 API 연동

---

## 4. 사용자 편집

### 4.1 필드 업데이트
```typescript
const updateField = <K extends keyof MatchPost>(
  key: K,
  value: MatchPost[K]
) => {
  setEditedPost((prev) => {
    if (!prev) return null;
    return { ...prev, [key]: value };
  });
};
```

### 4.2 편집 가능한 필드
- 제목 (현재는 disabled)
- 도움 카테고리 (`selectedTags`)
- 날짜/시간 정보 (DAY 또는 TERM 타입에 따라)
- 1회 제공 꿀 (`reward`)
- 만남 장소 (`location`)

---

## 5. 매칭확인서 생성 요청

### 5.1 확인 버튼 클릭

**핸들러**: `handleConfirm`

**유효성 검사**:
```typescript
if (!editedPost || !postDetail || !activeRoom || !user) {
  alert("필수 정보가 누락되었습니다.");
  return;
}
```

### 5.2 helperId와 disabledId 결정

**현재 로직** (임시):
```typescript
const myId = Number(activeRoom.myId);
const otherId = Number(activeRoom.otherId);

// 게시글 작성자는 disabled, 상대방은 helper로 가정
const helperId = otherId;   // 상대방이 도우미
const disabledId = myId;     // 현재 사용자가 장애인
```

**TODO**: 실제 역할에 따라 올바르게 설정 필요

### 5.3 engagementTime 변환

#### DAY 타입
```typescript
if (editedPost.type === "day") {
  // 날짜에서 요일 계산
  const dayOfWeekMap = {
    0: "SUNDAY", 1: "MONDAY", 2: "TUESDAY",
    3: "WEDNESDAY", 4: "THURSDAY", 5: "FRIDAY", 6: "SATURDAY"
  };
  const dayOfWeek = dayOfWeekMap[editedPost.date.getDay()] || "MONDAY";

  engagementTime = {
    date: formatDate(editedPost.date),  // "2025-12-28"
    schedule: {
      dayOfWeek: dayOfWeek,
      startTime: formatTime(editedPost.startTime),  // "10:00:00"
      endTime: formatTime(editedPost.endTime),      // "12:00:00"
    },
  };
}
```

#### TERM 타입
```typescript
else {
  engagementTime = {
    startDate: formatDate(editedPost.periodStart),  // "2026-01-01"
    endDate: formatDate(editedPost.periodEnd),      // "2026-01-31"
    schedules: editedPost.weeks.map((week) => ({
      dayOfWeek: convertDayNameToDayOfWeek(week.day),  // "월" → "MONDAY"
      startTime: formatTime(week.start),               // "09:00:00"
      endTime: formatTime(week.end),                   // "11:00:00"
    })),
  };
}
```

**날짜/시간 포맷 함수**:
```typescript
// Date → "YYYY-MM-DD"
const formatDate = (date: Date | null): string => {
  if (!date) return "";
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, "0");
  const day = String(date.getDate()).padStart(2, "0");
  return `${year}-${month}-${day}`;
};

// Date → "HH:mm:ss"
const formatTime = (date: Date | null): string => {
  if (!date) return "00:00:00";
  const hours = String(date.getHours()).padStart(2, "0");
  const minutes = String(date.getMinutes()).padStart(2, "0");
  return `${hours}:${minutes}:00`;
};
```

### 5.4 AgreementRequest 생성

**요청 데이터 구조**:
```typescript
const agreementRequest: AgreementRequest = {
  postId: Number(activeRoom.postId),           // 채팅방에서 가져온 게시글 ID
  helperId: helperId,                         // 도우미 ID
  disabledId: disabledId,                      // 장애인 ID
  type: editedPost.type === "day" ? "DAY" : "TERM",
  isVolunteer: false,                          // TODO: 실제 값으로 설정
  helpCategoryIds: selectedTags.length > 0 
    ? selectedTags 
    : postDetail.helpCategoryIds,               // 선택된 도움 카테고리
  unitHoney: editedPost.reward,                // 1회 제공 꿀
  totalHoney: editedPost.type === "day"
    ? editedPost.reward
    : editedPost.reward * (editedPost.weeks?.length || 1),  // 총 꿀 (임시 계산)
  region: editedPost.location || postDetail.postLegalDongCode,  // 만남 장소
  engagementTime: engagementTime,              // DAY 또는 TERM 타입에 따른 시간 정보
};
```

### 5.5 API 호출

**엔드포인트**:
```typescript
POST /match/agreements
```

**요청 본문**: `AgreementRequest`

**응답 데이터 구조** (`AgreementResponse`):
```typescript
{
  agreementId: number;
  status: "BEFORE" | "AFTER" | "CANCEL";
  confirmationDate: string;  // "YYYY-MM-DD"
  type: "DAY" | "TERM";
  isVolunteer: boolean;
  helpCategories: HelpCategory[];
  unitHoney: number;
  totalHoney: number;
  region: string;
  isDayComplete: boolean;
  isTermComplete: boolean;
}
```

### 5.6 성공 후 처리

```typescript
const response = await createAgreement(agreementRequest);
console.log("✅ [handleConfirm] 매칭 확인서 생성 성공:", response);

// 성공 시 채팅방으로 이동
navigate(`/chat/${chatroomId}`);
```

---

## 6. 데이터 흐름 다이어그램

```
[채팅방 페이지]
    ↓ "매칭하기" 버튼 클릭
[MatchFormPage 진입]
    ↓
[1] chatroomId 추출 (URL 파라미터)
    ↓
[2] 채팅방 정보 조회 (chatApi.openChatRoom)
    ├─ postId 획득
    └─ activeRoom 스토어에 저장
    ↓
[3] 게시글 상세 정보 조회 (postApi.getPostDetail)
    ├─ PostDetailResponse 수신
    └─ MatchPost로 변환
    ↓
[4] UI 렌더링
    ├─ 공통 필드 표시
    ├─ DAY/TERM 타입별 폼 표시
    └─ 사용자 편집 대기
    ↓
[5] 사용자 편집
    ├─ 필드 수정 (updateField)
    └─ 상태 업데이트 (editedPost, selectedTags)
    ↓
[6] "확인" 버튼 클릭
    ├─ 유효성 검사
    ├─ helperId/disabledId 결정
    ├─ engagementTime 변환
    ├─ AgreementRequest 생성
    └─ API 호출 (createAgreement)
    ↓
[7] 성공 응답 수신
    └─ 채팅방으로 이동
```

---

## 7. 주요 타입 정의

### 7.1 MatchPost (UI 편집용)
```typescript
interface MatchPost {
  type: "day" | "long";
  title: string;
  reward: number;
  location: string;
  
  // DAY 타입
  date?: Date | null;
  startTime?: Date | null;
  endTime?: Date | null;
  
  // TERM 타입
  periodStart?: Date | null;
  periodEnd?: Date | null;
  weeks?: {
    day: string;
    start: Date | null;
    end: Date | null;
  }[];
}
```

### 7.2 AgreementRequest (서버 요청용)
```typescript
interface AgreementRequest {
  postId: number;
  helperId: number;
  disabledId: number;
  type: "DAY" | "TERM";
  isVolunteer: boolean;
  helpCategoryIds: number[];
  unitHoney: number;
  totalHoney: number;
  region: string;
  engagementTime: DayEngagementTime | TermEngagementTime;
}
```

### 7.3 DayEngagementTime
```typescript
interface DayEngagementTime {
  date: string;  // "YYYY-MM-DD"
  schedule: {
    dayOfWeek: "MONDAY" | "TUESDAY" | ...;
    startTime: string;  // "HH:mm:ss"
    endTime: string;   // "HH:mm:ss"
  };
}
```

### 7.4 TermEngagementTime
```typescript
interface TermEngagementTime {
  startDate: string;  // "YYYY-MM-DD"
  endDate: string;   // "YYYY-MM-DD"
  schedules: {
    dayOfWeek: "MONDAY" | "TUESDAY" | ...;
    startTime: string;  // "HH:mm:ss"
    endTime: string;   // "HH:mm:ss"
  }[];
}
```

---

## 8. 에러 처리

### 8.1 초기 로딩 실패
```typescript
catch (error) {
  console.error("정보를 불러오는데 실패했습니다:", error);
  alert("정보를 불러오는데 실패했습니다.");
  navigate(-1);  // 이전 페이지로 이동
}
```

### 8.2 매칭확인서 생성 실패
```typescript
catch (error) {
  console.error("❌ [handleConfirm] 매칭 확인서 생성 실패:", error);
  alert("매칭 확인서 생성에 실패했습니다.");
  setIsSubmitting(false);  // 재시도 가능하도록 상태 초기화
}
```

---

## 9. 주요 함수 및 유틸리티

### 9.1 convertPostToMatchPost
- **목적**: `PostDetailResponse` → `MatchPost` 변환
- **로직**: `engagementType`에 따라 DAY/TERM 분기 처리

### 9.2 convertDayNameToDayOfWeek
- **목적**: 한글 요일명 → 영문 요일명 변환
- **예시**: "월" → "MONDAY"

### 9.3 formatDate
- **목적**: `Date` 객체 → "YYYY-MM-DD" 문자열 변환

### 9.4 formatTime
- **목적**: `Date` 객체 → "HH:mm:ss" 문자열 변환

### 9.5 updateField
- **목적**: `MatchPost` 객체의 특정 필드 업데이트
- **타입 안전성**: TypeScript 제네릭으로 타입 체크

---

## 10. 개선 사항 (TODO)

1. **helperId/disabledId 결정 로직**
   - 현재: 임시로 `otherId`를 helper, `myId`를 disabled로 설정
   - 개선: 게시글 작성자 정보를 확인하여 실제 역할에 따라 설정

2. **isVolunteer 값 설정**
   - 현재: 하드코딩된 `false`
   - 개선: 실제 값으로 설정

3. **totalHoney 계산 로직**
   - 현재: TERM 타입에서 `reward * weeks.length` (임시)
   - 개선: 실제 계산 로직 필요

4. **에러 처리 강화**
   - 네트워크 오류, 서버 오류 등 세분화된 에러 처리

---

## 11. 관련 파일

- **컴포넌트**: `src/domain/chat/pages/MatchFormPage.tsx`
- **API**: `src/domain/chat/api/agreementApi.ts`
- **타입**: `src/domain/chat/agreement.types.ts`
- **변환 유틸**: `src/domain/chat/components/matchConfirm/matchPost.ts`
- **게시글 API**: `src/api/postApi.ts`
- **채팅방 API**: `src/domain/chat/api/chatApi.ts`
- **스토어**: `src/domain/chat/store/useChatStore.ts`

---

## 12. 참고사항

- 매칭확인서 생성 후 자동으로 채팅방으로 이동합니다.
- 생성된 매칭확인서는 `agreementId`를 통해 조회할 수 있습니다.
- 매칭확인서 생성은 한 번만 가능하며, 이미 생성된 경우 재생성할 수 없습니다.


























