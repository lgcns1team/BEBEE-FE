{
  /*
<렌더링 구조>
- role에 따라 다른 마이페이지 컴포넌트를 렌더링합니다.

const { role } = useUserStore(); // role: 'DISABLED', 'HELPER'

if (role === "DISABLED") {
  return <DisabledMypage />;
}
if (role === "HELPER") {
  return <HelperMypage />;
}
*/
}
