# 채팅 도메인 Screen Reader 접근성 적용

## 📋 목차

1. [개요](#개요)
2. [적용 배경](#적용-배경)
3. [.sr-only 클래스](#sr-only-클래스)
4. [적용된 컴포넌트 및 페이지](#적용된-컴포넌트-및-페이지)
5. [적용 내용 상세](#적용-내용-상세)
6. [접근성 개선 효과](#접근성-개선-효과)
7. [참고 사항](#참고-사항)

---

## 개요

채팅 도메인(`domain/chat`)의 모든 페이지와 컴포넌트에 시각장애인 사용자를 위한 Screen Reader 접근성을 적용했습니다. 시각적으로 보이지 않는 설명을 `.sr-only` 클래스를 사용하여 추가하여 스크린 리더로 접근할 수 있도록 개선했습니다.

**작업 일시**: 2025-01-11  
**작업 범위**: `src/domain/chat/` 하위의 모든 페이지 및 컴포넌트  
**적용 기준**: 시각장애인이 보지 못하는 요소에 대한 설명 추가

---

## 적용 배경

장애인 매칭 플랫폼의 특성상 접근성은 매우 중요한 요소입니다. 특히 채팅 기능은 사용자 간 소통의 핵심 기능이므로, 시각장애인 사용자도 동일한 정보에 접근할 수 있어야 합니다.

### 적용 원칙

- 시각적으로 보이는 텍스트와 겹치지 않도록 `.sr-only` 클래스 사용
- 스크린 리더 사용자가 이해할 수 있는 명확한 설명 제공
- 버튼, 입력 필드, 상태 정보 등에 적절한 `aria-label` 및 `.sr-only` 설명 추가

---

## .sr-only 클래스

`.sr-only` 클래스는 `GlobalStyle.ts`에 정의되어 있으며, 시각적으로는 숨겨지지만 스크린 리더는 읽을 수 있는 텍스트를 제공합니다.

**위치**: `src/styles/GlobalStyle.ts`

```css
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```

---

## 적용된 컴포넌트 및 페이지

### 페이지 (Pages)

1. **ChatListPage.tsx** - 채팅 목록 페이지
2. **ChatRoomPage.tsx** - 채팅방 페이지
3. **MatchFormPage.tsx** - 매칭 확인서 작성 페이지

### 컴포넌트 (Components)

1. **ChatInput.tsx** - 채팅 입력 컴포넌트
2. **MessageItem.tsx** - 메시지 아이템 컴포넌트
3. **MeessageList.tsx** - 메시지 목록 컴포넌트
4. **ChatRoomCard.tsx** - 채팅방 카드 컴포넌트 (이미 적용됨)
5. **MatchResultCard.tsx** - 매칭 확인서 카드 컴포넌트 (이미 적용됨)
6. **MatchSuccessCard.tsx** - 매칭 성공 카드 컴포넌트
7. **MatchFailCard.tsx** - 매칭 실패 카드 컴포넌트 (이미 적용됨)
8. **PayReceipt.tsx** - 결제 영수증 컴포넌트

### 매칭 확인서 폼 컴포넌트

9. **DayHelpForm.tsx** - 하루 도움 폼
10. **LongHelpForm.tsx** - 지속 도움 폼
11. **InsufficientHoneyModal.tsx** - 꿀 부족 모달

---

## 적용 내용 상세

### 1. ChatInput.tsx

**적용 내용**:
- 입력 영역에 대한 설명 추가
- 전송 버튼의 상태 및 동작 설명 추가
- 아이콘에 `aria-hidden="true"` 적용

**예시 코드**:
```tsx
<InputArea role="region" aria-label="메시지 입력 영역">
  <span className="sr-only">채팅 메시지를 입력하는 영역입니다</span>
  <StyledInput
    aria-label="메시지 입력 필드"
    // ...
  />
  <SendButton
    aria-label={text.trim() ? "메시지 전송하기" : "메시지 전송하기 (메시지를 입력해주세요)"}
  >
    <SendIcon aria-hidden="true" />
    <span className="sr-only">
      {text.trim()
        ? "입력한 메시지를 전송합니다. Enter 키 또는 Space 키를 누르면 실행됩니다."
        : "메시지를 입력하면 전송할 수 있습니다."}
    </span>
  </SendButton>
</InputArea>
```

### 2. MessageItem.tsx

**적용 내용**:
- 메시지 발신자 정보 설명 추가
- 메시지 시간 정보 설명 추가
- 내가 보낸 메시지/받은 메시지 구분 설명

**예시 코드**:
```tsx
<MessageRow $isMe={isMe} role="listitem" aria-label={isMe ? "내가 보낸 메시지" : "받은 메시지"}>
  <MessageTime aria-label={`전송 시간: ${formatTime(message.createdAt)}`}>
    {formatTime(message.createdAt)}
  </MessageTime>
  <MessageBubble $isMe={isMe}>
    {message.textContent}
    <span className="sr-only">
      {isMe ? "내가 보낸 메시지" : "상대방이 보낸 메시지"}, 전송 시간: {formatTime(message.createdAt)}
    </span>
  </MessageBubble>
</MessageRow>
```

### 3. MeessageList.tsx

**적용 내용**:
- 메시지 목록 전체 설명 추가
- 날짜 구분선 설명 추가
- 메시지 개수 정보 제공

**예시 코드**:
```tsx
<ListContainer
  ref={scrollRef}
  role="log"
  aria-label="채팅 메시지 목록"
  aria-live="polite"
  aria-atomic="false"
>
  <span className="sr-only">
    채팅 메시지 목록입니다. 총 {messages.length}개의 메시지가 있습니다.
  </span>
  {isNewDay && (
    <DateDivider role="separator" aria-label={`날짜 구분선: ${getFormatDate(msg.createdAt)}`}>
      <span>{getFormatDate(msg.createdAt)}</span>
      <span className="sr-only">
        {getFormatDate(msg.createdAt)}부터의 메시지입니다
      </span>
    </DateDivider>
  )}
</ListContainer>
```

### 4. ChatRoomPage.tsx

**적용 내용**:
- 채팅방 페이지 전체 설명 추가

**예시 코드**:
```tsx
<ChatRoomLayout role="main" aria-label="채팅방">
  <span className="sr-only">채팅방 페이지입니다. 메시지를 주고받을 수 있습니다.</span>
  {/* ... */}
</ChatRoomLayout>
```

### 5. MatchFormPage.tsx

**적용 내용**:
- 페이지 전체 설명 추가
- 잔액 확인 버튼 설명 추가
- 폼 섹션 설명 개선

**예시 코드**:
```tsx
<div role="main" aria-label="매칭 확인서 작성">
  <span className="sr-only">
    매칭 확인서 작성 페이지입니다. 도움 유형, 날짜, 시간, 장소 등을 입력하여 매칭 확인서를 작성할 수 있습니다.
  </span>
  {/* ... */}
  <BalanceCheckButton
    aria-label={
      isCheckingBalance
        ? "잔액 확인 중"
        : agreementRequest.isVolunteer
        ? "나눔은 잔액 확인이 필요 없습니다"
        : "잔액 확인하기"
    }
  >
    {isCheckingBalance ? "확인 중..." : "잔액확인"}
    <span className="sr-only">
      {isCheckingBalance
        ? "잔액을 확인하는 중입니다"
        : agreementRequest.isVolunteer
        ? "나눔은 꿀이 차감되지 않으므로 잔액 확인이 필요 없습니다"
        : "보유한 꿀 잔액을 확인합니다. Enter 키 또는 Space 키를 누르면 실행됩니다."}
    </span>
  </BalanceCheckButton>
</div>
```

### 6. MatchSuccessCard.tsx

**적용 내용**:
- 매칭 성공 메시지 설명 추가
- 이모지에 `aria-hidden="true"` 적용

**예시 코드**:
```tsx
<Container role="region" aria-label="매칭 성공 알림">
  <Title>
    <span aria-hidden="true">🎉</span> 짝짝짝! 매칭이 성사되었어요
    <span className="sr-only">
      매칭이 성사되었습니다. 확정 내용은 매칭 현황에서도 확인할 수 있습니다.
    </span>
  </Title>
  {/* ... */}
</Container>
```

### 7. PayReceipt.tsx

**적용 내용**:
- 영수증 제목 및 설명 추가
- 사용액, 잔액 정보 설명 추가
- 날짜 정보 설명 추가

**예시 코드**:
```tsx
<ReceiptContainer role="contentinfo" aria-label="꿀 사용 영수증">
  <ReceiptTitle>
    꿀 사용 영수증
    <span className="sr-only">매칭 성사로 인한 꿀 사용 내역 영수증입니다</span>
  </ReceiptTitle>
  <ReceiptBody role="group" aria-label="꿀 사용 내역 상세">
    <ReceiptItem>
      <ItemLabel>사용액</ItemLabel>
      <ItemValue>
        {usedHoney.toLocaleString()} 꿀
        <span className="sr-only">사용된 꿀은 {usedHoney.toLocaleString()}꿀입니다</span>
      </ItemValue>
    </ReceiptItem>
  </ReceiptBody>
</ReceiptContainer>
```

### 8. DayHelpForm.tsx

**적용 내용**:
- 날짜 선택 필드 설명 추가
- 시간 선택 필드 설명 추가
- 선택된 값에 대한 설명 추가
- 아이콘 버튼에 `aria-label` 추가

**예시 코드**:
```tsx
<FieldSet role="group" aria-label="하루 도움 날짜 및 시간 입력">
  <ModalLabel>
    도움 날짜 {required && <RequiredMark>*</RequiredMark>}
    <span className="sr-only">필수 입력 항목입니다</span>
  </ModalLabel>
  <CalendarIconWrapper
    onClick={handleCalendarIconClick}
    aria-label="날짜 선택 캘린더 열기"
  >
    <CiCalendar size={20} aria-hidden="true" />
  </CalendarIconWrapper>
  <span className="sr-only">
    {dayEngagement.date
      ? `선택된 날짜: ${dayEngagement.date.toLocaleDateString("ko-KR")}`
      : "날짜를 선택해주세요"}
  </span>
</FieldSet>
```

### 9. LongHelpForm.tsx

**적용 내용**:
- 기간 선택 필드 설명 추가
- 스케줄 목록 설명 추가
- 스케줄 추가/삭제 버튼 설명 추가
- 요일 및 시간 선택 필드 설명 추가

**예시 코드**:
```tsx
<FieldSet role="group" aria-label="도움 요일 및 시간 입력">
  {weeks.length > 0 && (
    <span className="sr-only">등록된 스케줄 {weeks.length}개가 있습니다</span>
  )}
  {weeks.map((week, index) => (
    <ScheduleBox key={index} role="group" aria-label={`스케줄 ${index + 1}`}>
      <CloseButtonWrapper
        onClick={() => handleDeleteSchedule(index)}
        aria-label={`스케줄 ${index + 1} 삭제하기`}
      >
        <IoClose size={20} aria-hidden="true" />
        <span className="sr-only">이 스케줄을 삭제합니다</span>
      </CloseButtonWrapper>
      {/* ... */}
    </ScheduleBox>
  ))}
</FieldSet>
```

### 10. InsufficientHoneyModal.tsx

**적용 내용**:
- 모달 제목 및 설명 추가
- 잔액 정보 설명 추가
- 버튼 설명 추가
- 모달 속성 설정 (`role="dialog"`, `aria-modal="true"` 등)

**예시 코드**:
```tsx
<Modal
  role="dialog"
  aria-modal="true"
  aria-labelledby="insufficient-honey-title"
  aria-describedby="insufficient-honey-description"
>
  <span className="sr-only">꿀 부족 알림 모달입니다</span>
  <Title id="insufficient-honey-title">
    앗! 꿀이 부족해요
    <span className="sr-only">
      매칭 확인서를 작성하기에 보유한 꿀이 부족합니다
    </span>
  </Title>
  <RechargeButton
    onClick={handleRecharge}
    aria-label="꿀 충전 페이지로 이동"
  >
    <HoneyIcon src={honey} alt="" aria-hidden="true" />
    꿀 충전하기
    <span className="sr-only">
      꿀 충전 페이지로 이동합니다. Enter 키 또는 Space 키를 누르면 실행됩니다.
    </span>
  </RechargeButton>
</Modal>
```

---

## 접근성 개선 효과

### 개선 전

- 시각장애인 사용자가 채팅 메시지의 발신자, 시간 정보를 파악하기 어려움
- 버튼의 기능을 명확히 알 수 없음
- 폼 입력 필드의 상태를 확인하기 어려움
- 모달, 카드 등의 콘텐츠 구조를 이해하기 어려움

### 개선 후

- 모든 주요 UI 요소에 대한 명확한 설명 제공
- 스크린 리더를 통한 정보 접근성 향상
- 키보드 네비게이션과 함께 사용 가능한 설명 제공
- 상태 정보(로딩, 성공, 실패 등)를 명확히 전달

### 적용 효과

1. **정보 접근성 향상**: 시각장애인 사용자도 채팅 기능을 동일하게 이용 가능
2. **사용성 개선**: 명확한 설명으로 기능 이해도 향상
3. **WCAG 준수**: 웹 접근성 지침 준수를 위한 기반 마련
4. **법적 요구사항 준수**: 장애인 차별금지법 준수

---

## 참고 사항

### 주의사항

1. **중복 설명 방지**: 시각적으로 이미 명확한 텍스트가 있는 경우, `.sr-only` 설명이 중복되지 않도록 주의
2. **동적 콘텐츠**: 상태가 변경되는 요소는 `aria-live` 속성과 함께 사용
3. **아이콘 처리**: 의미를 전달하지 않는 장식용 아이콘은 `aria-hidden="true"` 적용
4. **키보드 접근성**: `.sr-only` 설명과 함께 키보드 이벤트 핸들러(`onKeyDown`)도 함께 구현

### 모범 사례

1. **명확한 설명**: "버튼"보다는 "메시지 전송하기"처럼 구체적인 설명 제공
2. **상태 정보 포함**: 버튼의 활성/비활성 상태를 설명에 포함
3. **콘텍스트 제공**: 단순히 "확인"보다는 "매칭 확인서 작성 완료 및 전송"처럼 의미 명확화
4. **키보드 단축키 안내**: Enter 키 또는 Space 키 사용 안내 포함

### 향후 개선 사항

1. **다른 도메인 확장**: 채팅 도메인 외 다른 도메인에도 접근성 적용
2. **테스트 도구 활용**: 스크린 리더 테스트 도구를 사용한 실제 테스트 수행
3. **사용자 피드백**: 실제 시각장애인 사용자 피드백 수집 및 반영
4. **접근성 가이드 문서화**: 팀 내 접근성 가이드라인 문서 작성

---

**작성일**: 2025-01-11  
**작성자**: AI Assistant  
**버전**: 1.0
