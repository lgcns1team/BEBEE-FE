# 프로젝트 회고

## 어려웠던 점

### 1. 배포 환경에서의 undefined/null 오류 처리

- **문제**: 배포 환경에서 데이터가 없을 때 `Cannot read properties of undefined (reading 'length')` 오류 발생
- **원인**:
  - localStorage 데이터 손상 또는 구조 불일치
  - 초기 데이터 부재 시 안전한 처리 부족
  - API 응답 데이터 구조 예상과 다른 경우
- **해결 과정**:
  - 모든 배열 접근 전 `Array.isArray()` 체크 추가
  - localStorage 데이터 검증 및 초기화 로직 강화
  - Optional Chaining(`?.`)과 Nullish Coalescing(`??`) 연산자 활용
  - 안전한 기본값(`[]`) 제공

### 2. 타입 중복 및 불일치 문제

- **문제**: 여러 파일에서 동일한 타입/상수/함수가 중복 정의됨
  - 타입 중복: HelpType, DayOfWeek, Gender 등
  - 상수 중복: 요일 매핑 객체가 5개 이상 중복
  - 함수 중복: 날짜/시간 포맷팅 함수가 10개 이상 중복
- **원인**: 도메인별로 독립적으로 개발하며 공통 코드가 분산됨
- **해결 과정**:
  - `common.types.ts` 파일로 통합
  - 함수 시그니처 차이를 용도별로 명확히 구분 (`formatDateToISO`, `formatDateToKorean` 등)
  - 기존 코드와의 호환성을 위해 재export 활용

### 3. 소켓 통신과 매칭확인서 생성 로직의 복잡성

- **문제**:
  - 소켓 발행 규칙 혼란 (텍스트만? 매칭확인서도?)
  - 매칭확인서 생성 후 소켓으로 받는 과정의 이해 어려움
  - 재입장 시 메시지 동기화 처리
- **원인**: 소켓 통신 규칙이 문서화되지 않아 혼란 발생
- **해결 과정**:
  - 명확한 규칙 수립: 프론트에서는 텍스트 메시지만 소켓 발행
  - 매칭확인서는 POST API로 생성하고, 서버에서 소켓 발행
  - 재입장 시 GET API로 메시지 조회하도록 변경

### 4. React 렌더링 규칙 위반

- **문제**: `Date.now()`를 key로 사용하여 렌더링 중 부수 효과 발생
- **원인**: 고유 key 생성 로직에서 순수 함수 원칙 미준수
- **해결**: 문자열 기반 고유 key 생성으로 변경

### 5. 매칭확인서 생성 과정의 복잡한 데이터 변환

- **문제**: 매칭확인서 생성 시 여러 단계의 데이터 변환이 필요했음
  - `PostDetailResponse` → `MatchPost` 변환 (서버 응답 → UI 편집용)
  - `MatchPost` → `AgreementRequest` 변환 (UI 편집용 → 서버 요청)
  - DAY/TERM 타입별 다른 처리 로직
  - Date 객체 ↔ 문자열 변환 (ISO 형식)
  - 날짜에서 요일 계산 (`Date.getDay()` → `DayOfWeek`)
- **원인**:
  - 서버와 프론트 간 데이터 구조 차이
  - UI 편집을 위한 중간 변환 단계 필요
  - 타입별로 다른 구조 (DAY는 단일 날짜, TERM은 기간 + 주간 스케줄)
- **해결 과정**:
  - `convertPostToMatchPost` 함수로 서버 데이터를 UI 편집용으로 변환
  - DAY/TERM 타입별 분기 처리로 명확한 로직 구분
  - 공통 유틸리티 함수 활용 (`formatDateToISO`, `getDayOfWeekFromDate` 등)
  - 문서화를 통한 데이터 흐름 명확화

### 6. 매칭확인서 생성 시 비즈니스 로직의 불명확성

- **문제**:
  - `helperId`와 `disabledId` 결정 로직이 임시로 구현됨
  - `totalHoney` 계산 로직이 타입별로 다르고 불완전함
  - `isVolunteer` 값이 하드코딩되어 있음
- **원인**: 비즈니스 요구사항 명확화 전 구현 진행
- **해결 과정**:
  - TODO 주석으로 개선 사항 명시
  - 문서화를 통해 향후 개선 필요 사항 정리

## 배운 점

### 1. 안전한 배열/객체 처리 패턴

```typescript
// 항상 배열 체크
const safeMessages = Array.isArray(messages) ? messages : [];

// Optional Chaining 활용
const count = messages?.length ?? 0;

// 데이터 검증
if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
  return defaultValue;
}
```

- **핵심 원칙**:
  - 모든 외부 데이터(API, localStorage)는 검증 후 사용
  - 배열/객체 접근 전 타입 체크 필수
  - 기본값 명시적 제공

### 2. 타입 통합 및 코드 중복 제거

- **효과**:
  - 타입 중복 66% 감소 (3개 → 1개)
  - 상수 중복 80% 이상 감소
  - 함수 중복 90% 이상 감소
- **학습 내용**:
  - 공통 타입/유틸리티는 한 곳에 집중 관리
  - 명확한 네이밍으로 용도 구분 (`formatDateToISO` vs `formatDateToKorean`)
  - 기존 코드 호환성을 위한 재export 패턴

### 3. 소켓 통신 아키텍처 이해

- **배운 내용**:
  - 프론트는 텍스트 메시지만 발행, 나머지는 서버에서 발행
  - Optimistic Update 패턴으로 즉시 UI 반영
  - 메시지 ID 기반 중복 방지 로직
  - 재입장 시 API 조회 후 소켓 구독으로 실시간 동기화

### 4. 상태 관리 구조화

- **Zustand 스토어 역할 분리**:
  - `useSocketStore`: 소켓 연결 및 메시지 전송만 담당
  - `useChatStore`: 채팅방별 메시지 상태 관리
  - Store 내에서 직접 API 호출 금지 (useSocketStore 예외)
- **localStorage 동기화**:
  - 데이터 검증 후 저장/로드
  - 오류 시 안전한 초기화

### 5. 프로젝트 컨벤션의 중요성

- **도메인 구조 규칙**:
  - 도메인별로 components, hooks, pages, utils 분리
  - 공통 타입은 `common.types.ts`로 통합
  - 파일명 네이밍 규칙 명확화 (kebab-case, PascalCase 등)
- **코드 일관성**:
  - import 순서 규칙 (React → 외부 라이브러리 → 내부 → 스타일)
  - props 순서 규칙 (boolean → 기타 → 핸들러)
  - 함수 네이밍 규칙 (handle*, create*, get*, format* 등)

### 6. 문서화의 가치

- **트러블슈팅 문서**:
  - 문제 상황, 원인 분석, 해결 방법을 상세히 기록
  - 향후 유사 문제 발생 시 빠른 해결 가능
- **로직 정리 문서**:
  - 매칭확인서 생성 과정, 채팅 로직 흐름도를 통한 명확한 이해
  - 새 팀원 온보딩 시 도움

### 7. 매칭확인서 생성 로직의 데이터 변환 패턴

- **배운 내용**:

  - **3단계 변환 구조**: 서버 응답 → UI 편집용 → 서버 요청
    - `PostDetailResponse` (서버 응답 구조)
    - `MatchPost` (UI 편집 가능한 구조)
    - `AgreementRequest` (서버 요청 구조)
  - **타입별 분기 처리**:
    - DAY 타입: 단일 날짜 + 스케줄
    - TERM 타입: 기간(시작일/종료일) + 주간 스케줄 배열
  - **날짜/시간 변환 패턴**:

    ```typescript
    // Date 객체 → ISO 문자열
    const dateStr = formatDateToISO(date); // "2025-01-05"
    const timeStr = formatTimeToISO(date); // "14:30:00"

    // 날짜에서 요일 추출
    const dayOfWeek = getDayOfWeekFromDate(date); // "MONDAY"

    // 한글 요일명 → 영문 변환
    const dayOfWeek = convertDayNameToDayOfWeek("월"); // "MONDAY"
    ```

- **학습 내용**:
  - 복잡한 데이터 변환은 명확한 함수로 분리
  - 변환 단계별로 중간 타입 정의로 타입 안정성 확보
  - 공통 유틸리티 함수 활용으로 중복 제거

### 8. 복잡한 비즈니스 로직 구현 시 문서화의 중요성

- **배운 내용**:
  - 매칭확인서 생성 과정처럼 여러 단계가 있는 경우 문서화가 필수
  - 데이터 흐름도를 통해 전체 프로세스 이해 용이
  - TODO 주석으로 향후 개선 사항 명확히 기록
- **효과**:
  - 향후 비즈니스 로직 변경 시 빠른 파악 가능
  - 코드 리뷰 시 의도 파악 용이
  - 유지보수 시 컨텍스트 손실 최소화

## 개선 효과

### 코드 품질

- ✅ 타입 안정성 향상 (명시적 타입 가드 사용)
- ✅ 에러 방지 (undefined/null 체크 강화)
- ✅ 유지보수성 향상 (중복 코드 제거)
- ✅ 가독성 향상 (명확한 네이밍, 구조화)

### 개발 경험

- ✅ 일관된 코딩 스타일로 협업 효율성 향상
- ✅ 트러블슈팅 시간 단축 (문서화된 해결 방법)
- ✅ 새로운 기능 추가 시 빠른 개발 (공통 유틸리티 활용)
