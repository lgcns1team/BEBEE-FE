# 접근성 유틸리티 사용법

## 📋 목차

1. [개요](#개요)
2. [유틸리티 함수 목록](#유틸리티-함수-목록)
3. [사용법](#사용법)
4. [실전 예제](#실전-예제)
5. [주의사항](#주의사항)
6. [모범 사례](#모범-사례)

---

## 개요

접근성 유틸리티(`src/utils/accessibility.ts`)는 시각장애인 사용자를 위한 Screen Reader 접근성을 쉽게 구현할 수 있도록 돕는 공통 유틸리티 함수들입니다.

**위치**: `src/utils/accessibility.ts`

### 주요 기능

- 키보드 이벤트 핸들러 생성 (Enter, Space 키 지원) - 주의: 모바일 웹에서는 의미 제한적
- 접근성 속성 자동 추가 (aria-label, onKeyDown 등)
- Screen Reader용 텍스트 생성 (.sr-only 클래스) - 모바일에서 중요
- 아이콘 접근성 속성 추가 (aria-hidden)

### 모바일 웹 환경 고려사항

이 프로젝트는 **모바일 웹**으로 개발 중입니다. 모바일 환경에서는 다음 사항을 고려해야 합니다:

- ✅ **Screen Reader 지원**: iOS VoiceOver, Android TalkBack 등에서 `.sr-only` 텍스트와 `aria-label`이 정상 작동
- ⚠️ **키보드 접근성**: 모바일에서는 물리 키보드가 없으므로 `onKeyDown` 핸들러는 의미가 제한적 (외부 키보드 연결 시에만 유용)
- ✅ **터치 접근성**: `aria-label`로 터치 요소의 기능을 명확히 설명하는 것이 중요
- ✅ **포커스 관리**: 모바일에서는 터치로 포커스를 관리하지만, Screen Reader 사용자는 스와이프로 탐색

---

## 유틸리티 함수 목록

### 1. `createKeyboardHandler`

키보드 이벤트 핸들러를 생성하는 함수입니다. Enter 키 또는 Space 키를 누르면 지정된 핸들러를 실행합니다.

### 2. `createAccessibleButtonProps`

버튼에 접근성 속성을 쉽게 추가할 수 있도록 하는 함수입니다.

### 3. `createSrOnlyText`

Screen Reader용 텍스트 요소를 생성하는 함수입니다.

### 4. `createAccessibleIconProps`

장식용 아이콘에 `aria-hidden="true"` 속성을 추가하는 함수입니다.

---

## 사용법

### 1. createKeyboardHandler

**기본 사용법**

```tsx
import { createKeyboardHandler } from "../../../utils/accessibility";

const MyComponent = () => {
  const handleClick = () => {
    console.log("클릭됨");
  };

  return (
    <button
      onClick={handleClick}
      onKeyDown={createKeyboardHandler(handleClick)}
    >
      확인
    </button>
  );
};
```

**조건부 실행**

```tsx
const MyComponent = () => {
  const [isLoading, setIsLoading] = useState(false);

  const handleSubmit = () => {
    if (isLoading) return;
    setIsLoading(true);
    // 제출 로직
  };

  return (
    <button
      onClick={handleSubmit}
      onKeyDown={createKeyboardHandler(handleSubmit, !isLoading)}
      disabled={isLoading}
    >
      {isLoading ? "로딩 중..." : "제출"}
    </button>
  );
};
```

**파라미터 없이 실행**

```tsx
const MyComponent = () => {
  return (
    <div
      onClick={() => navigate("/home")}
      onKeyDown={createKeyboardHandler(() => navigate("/home"))}
      role="button"
      tabIndex={0}
    >
      홈으로 이동
    </div>
  );
};
```

---

### 2. createAccessibleButtonProps

**기본 사용법**

```tsx
import { createAccessibleButtonProps } from "../../../utils/accessibility";

const MyButton = () => {
  const handleClick = () => {
    console.log("메시지 전송");
  };

  const { props, srOnlyElement } = createAccessibleButtonProps(
    {
      ariaLabel: "메시지 전송하기",
      srOnlyText:
        "입력한 메시지를 전송합니다. Enter 키 또는 Space 키를 누르면 실행됩니다.",
    },
    handleClick
  );

  return (
    <button onClick={handleClick} {...props}>
      전송
      {srOnlyElement}
    </button>
  );
};
```

**키보드 핸들러 없이 사용**

```tsx
const MyButton = () => {
  const handleClick = () => {
    console.log("클릭");
  };

  const { props, srOnlyElement } = createAccessibleButtonProps(
    {
      ariaLabel: "취소하기",
      srOnlyText: "작업을 취소합니다.",
      enableKeyboardHandler: false, // 키보드 핸들러 비활성화
    },
    handleClick
  );

  return (
    <button onClick={handleClick} {...props}>
      취소
      {srOnlyElement}
    </button>
  );
};
```

**비활성화 상태 처리**

```tsx
const MyButton = () => {
  const [disabled, setDisabled] = useState(false);

  const handleClick = () => {
    console.log("클릭");
  };

  const { props, srOnlyElement } = createAccessibleButtonProps(
    {
      ariaLabel: disabled ? "버튼이 비활성화되어 있습니다" : "확인하기",
      srOnlyText: disabled
        ? "버튼이 비활성화되어 있습니다"
        : "확인 버튼을 누르면 작업을 진행합니다.",
    },
    handleClick,
    disabled
  );

  return (
    <button onClick={handleClick} {...props} disabled={disabled}>
      확인
      {srOnlyElement}
    </button>
  );
};
```

---

### 3. createSrOnlyText

**기본 사용법**

```tsx
import { createSrOnlyText } from "../../../utils/accessibility";

const MyComponent = () => {
  return (
    <div>
      <h2>
        매칭 성공
        {createSrOnlyText("매칭이 성공적으로 완료되었습니다.")}
      </h2>
    </div>
  );
};
```

**조건부 텍스트**

```tsx
const MyComponent = () => {
  const isSuccess = true;

  return (
    <div>
      <h2>
        {isSuccess ? "성공" : "실패"}
        {createSrOnlyText(
          isSuccess
            ? "작업이 성공적으로 완료되었습니다."
            : "작업에 실패했습니다. 다시 시도해주세요."
        )}
      </h2>
    </div>
  );
};
```

---

### 4. createAccessibleIconProps

**기본 사용법**

```tsx
import { createAccessibleIconProps } from "../../../utils/accessibility";
import { IoClose } from "react-icons/io5";

const MyComponent = () => {
  return (
    <button onClick={handleClose}>
      <IoClose {...createAccessibleIconProps({ size: 20 })} />
      닫기
    </button>
  );
};
```

**React Icons와 함께 사용**

```tsx
import { createAccessibleIconProps } from "../../../utils/accessibility";
import { CiCalendar } from "react-icons/ci";

const DatePicker = () => {
  return (
    <div>
      <CiCalendar {...createAccessibleIconProps({ size: 20 })} />
      <input type="date" />
    </div>
  );
};
```

---

## 실전 예제

### 예제 1: 채팅 전송 버튼

**Before (적용 전)**

```tsx
const ChatInput = () => {
  const handleSend = () => {
    // 전송 로직
  };

  return <button onClick={handleSend}>전송</button>;
};
```

**After (적용 후)**

```tsx
import { createAccessibleButtonProps } from "../../../utils/accessibility";

const ChatInput = () => {
  const [text, setText] = useState("");

  const handleSend = () => {
    if (!text.trim()) return;
    // 전송 로직
  };

  const { props, srOnlyElement } = createAccessibleButtonProps(
    {
      ariaLabel: text.trim()
        ? "메시지 전송하기"
        : "메시지 전송하기 (메시지를 입력해주세요)",
      srOnlyText: text.trim()
        ? "입력한 메시지를 전송합니다. Enter 키 또는 Space 키를 누르면 실행됩니다."
        : "메시지를 입력하면 전송할 수 있습니다.",
    },
    handleSend,
    !text.trim()
  );

  return (
    <button onClick={handleSend} {...props} disabled={!text.trim()}>
      전송
      {srOnlyElement}
    </button>
  );
};
```

---

### 예제 2: 매칭 확인서 수락/거절 버튼

**Before (적용 전)**

```tsx
const MatchResultCard = () => {
  const handleAccept = () => {
    // 수락 로직
  };

  const handleRefuse = () => {
    // 거절 로직
  };

  return (
    <div>
      <button onClick={handleRefuse}>거절</button>
      <button onClick={handleAccept}>수락</button>
    </div>
  );
};
```

**After (적용 후)**

```tsx
import { createAccessibleButtonProps } from "../../../utils/accessibility";

const MatchResultCard = () => {
  const handleAccept = () => {
    // 수락 로직
  };

  const handleRefuse = () => {
    // 거절 로직
  };

  const acceptProps = createAccessibleButtonProps(
    {
      ariaLabel: "매칭 확인서 수락하기",
      srOnlyText: "이 매칭 확인서를 수락하고 매칭을 완료합니다.",
    },
    handleAccept
  );

  const refuseProps = createAccessibleButtonProps(
    {
      ariaLabel: "매칭 확인서 거절하기",
      srOnlyText: "이 매칭 확인서를 거절합니다.",
    },
    handleRefuse
  );

  return (
    <div>
      <button onClick={handleRefuse} {...refuseProps.props}>
        거절
        {refuseProps.srOnlyElement}
      </button>
      <button onClick={handleAccept} {...acceptProps.props}>
        수락
        {acceptProps.srOnlyElement}
      </button>
    </div>
  );
};
```

---

### 예제 3: 날짜 선택기 아이콘

**Before (적용 전)**

```tsx
import { CiCalendar } from "react-icons/ci";

const DatePicker = () => {
  const handleIconClick = () => {
    // 날짜 선택기 열기
  };

  return (
    <div onClick={handleIconClick}>
      <CiCalendar size={20} />
      <input type="date" />
    </div>
  );
};
```

**After (적용 후)**

```tsx
import { createAccessibleIconProps } from "../../../utils/accessibility";
import { CiCalendar } from "react-icons/ci";

const DatePicker = () => {
  const handleIconClick = () => {
    // 날짜 선택기 열기
  };

  return (
    <div onClick={handleIconClick} aria-label="날짜 선택 캘린더 열기">
      <CiCalendar {...createAccessibleIconProps({ size: 20 })} />
      <input type="date" />
    </div>
  );
};
```

---

### 예제 4: 리스트 아이템 클릭

**Before (적용 전)**

```tsx
const ChatList = ({ chatrooms, onChatOpen }) => {
  return (
    <div>
      {chatrooms.map((room) => (
        <div key={room.id} onClick={() => onChatOpen(room.id)}>
          {room.title}
        </div>
      ))}
    </div>
  );
};
```

**After (적용 후)**

```tsx
import { createKeyboardHandler } from "../../../utils/accessibility";

const ChatList = ({ chatrooms, onChatOpen }) => {
  return (
    <div role="list" aria-label="채팅방 목록">
      {chatrooms.map((room) => (
        <div
          key={room.id}
          role="listitem"
          onClick={() => onChatOpen(room.id)}
          onKeyDown={createKeyboardHandler(() => onChatOpen(room.id))}
          aria-label={`${room.title} 채팅방, 마지막 메시지: ${
            room.lastMessage || "없음"
          }`}
          tabIndex={0}
        >
          {room.title}
        </div>
      ))}
    </div>
  );
};
```

---

## 주의사항

### 1. 모바일 웹 환경에서의 키보드 핸들러

**⚠️ 중요**: 이 프로젝트는 모바일 웹으로 개발 중입니다. 모바일 환경에서는 물리 키보드가 없으므로 `onKeyDown` 핸들러의 효과가 제한적입니다.

- **키보드 핸들러의 역할**: 외부 키보드 연결 시나 접근성 테스트 도구 사용 시에만 의미가 있음
- **모바일에서 중요**: `aria-label`, `.sr-only` 텍스트가 더 중요 (Screen Reader 사용)
- **권장사항**: 키보드 핸들러는 유지하되, 모바일에서의 주요 접근성은 `aria-label`과 `.sr-only`에 집중

```tsx
// 모바일 웹에서 권장하는 패턴
const { props, srOnlyElement } = createAccessibleButtonProps(
  {
    ariaLabel: "메시지 전송하기", // ✅ 모바일에서 매우 중요
    srOnlyText: "입력한 메시지를 전송합니다.", // ✅ 모바일에서 매우 중요
    enableKeyboardHandler: true, // ⚠️ 외부 키보드 연결 시에만 유용
  },
  handleSend
);
```

### 2. 키보드 핸들러는 클릭 가능한 요소에만 사용

- `button`, `a` 태그: 기본적으로 키보드 접근 가능 (선택적 사용)
- `div`, `span` 등: `role="button"`, `tabIndex={0}` 필요 (모바일에서는 터치로 작동)

```tsx
// ❌ 잘못된 사용
<div onClick={handleClick}>
  클릭
</div>

// ✅ 올바른 사용
<div
  onClick={handleClick}
  onKeyDown={createKeyboardHandler(handleClick)}
  role="button"
  tabIndex={0}
>
  클릭
</div>
```

### 2. disabled 상태 처리

버튼이 비활성화되어 있을 때는 키보드 핸들러도 실행되지 않도록 해야 합니다.

```tsx
// ✅ 올바른 사용
const { props } = createAccessibleButtonProps(
  { ariaLabel: "제출하기" },
  handleSubmit,
  disabled // disabled 상태 전달
);

<button onClick={handleSubmit} {...props} disabled={disabled}>
  제출
</button>;
```

### 3. 중복 설명 방지

시각적으로 이미 명확한 텍스트가 있는 경우, `.sr-only` 텍스트는 중복되지 않도록 주의합니다.

```tsx
// ❌ 중복 설명 (불필요)
<button aria-label="확인하기">
  확인하기
  {createSrOnlyText("확인하기 버튼")}
</button>

// ✅ 의미 있는 추가 설명
<button aria-label="매칭 확인서 수락하기">
  수락
  {createSrOnlyText("이 매칭 확인서를 수락하고 매칭을 완료합니다.")}
</button>
```

### 4. 아이콘 사용 시 주의

- 의미를 전달하는 아이콘: `aria-label` 사용
- 장식용 아이콘: `aria-hidden="true"` 사용

```tsx
// ✅ 의미를 전달하는 아이콘
<button aria-label="삭제하기">
  <DeleteIcon aria-label="삭제 아이콘" />
</button>

// ✅ 장식용 아이콘
<button aria-label="닫기">
  <CloseIcon {...createAccessibleIconProps({ size: 20 })} />
  닫기
</button>
```

---

## 모범 사례

### 1. 명확한 설명 제공

- "버튼"보다는 구체적인 기능 설명
- 상태 정보 포함 (활성/비활성, 로딩 중 등)
- 키보드 단축키 안내 포함

```tsx
// ✅ 좋은 예
createAccessibleButtonProps(
  {
    ariaLabel: "메시지 전송하기",
    srOnlyText:
      "입력한 메시지를 전송합니다. Enter 키 또는 Space 키를 누르면 실행됩니다.",
  },
  handleSend
);

// ❌ 나쁜 예
createAccessibleButtonProps(
  {
    ariaLabel: "버튼",
    srOnlyText: "클릭",
  },
  handleSend
);
```

### 2. 동적 설명 사용

버튼의 상태에 따라 설명을 변경합니다.

```tsx
const { props, srOnlyElement } = createAccessibleButtonProps(
  {
    ariaLabel: isLoading ? "제출 중" : "제출하기",
    srOnlyText: isLoading
      ? "제출 중입니다. 잠시만 기다려주세요."
      : "작성을 완료하고 제출합니다. Enter 키 또는 Space 키를 누르면 실행됩니다.",
  },
  handleSubmit,
  isLoading
);
```

### 3. 일관성 유지

팀 전체에서 동일한 패턴을 사용합니다.

```tsx
// 모든 버튼에 동일한 패턴 적용
const submitButton = createAccessibleButtonProps(
  { ariaLabel: "제출하기", srOnlyText: "..." },
  handleSubmit
);
const cancelButton = createAccessibleButtonProps(
  { ariaLabel: "취소하기", srOnlyText: "..." },
  handleCancel
);
```

---

## 모바일 웹 환경에서의 접근성 테스트

### Screen Reader 테스트

모바일 웹에서 접근성을 테스트하려면 실제 모바일 디바이스의 Screen Reader를 사용해야 합니다:

1. **iOS (VoiceOver)**

   - 설정 → 손쉬운 사용 → VoiceOver 활성화
   - Safari에서 웹사이트 접속
   - 스와이프로 요소 탐색
   - `.sr-only` 텍스트와 `aria-label` 확인

2. **Android (TalkBack)**
   - 설정 → 손쉬운 사용 → TalkBack 활성화
   - Chrome에서 웹사이트 접속
   - 스와이프로 요소 탐색
   - `.sr-only` 텍스트와 `aria-label` 확인

### 모바일에서 중요한 접근성 요소

1. **`aria-label`**: 터치 요소의 기능을 명확히 설명
2. **`.sr-only` 텍스트**: Screen Reader 사용자를 위한 추가 설명
3. **`role` 속성**: 요소의 역할 명시 (button, list, listitem 등)
4. **포커스 가능한 요소**: `tabIndex={0}` 설정 (Screen Reader 탐색용)

### 모바일에서 제한적인 요소

1. **`onKeyDown` 핸들러**: 물리 키보드가 없으므로 효과 제한적 (외부 키보드 연결 시에만 유용)

---

## 추가 리소스

- [WCAG 2.1 가이드라인](https://www.w3.org/WAI/WCAG21/quickref/)
- [ARIA 사양](https://www.w3.org/TR/wai-aria-1.1/)
- [React 접근성 가이드](https://react.dev/learn/accessibility)
- [모바일 접근성 가이드](https://www.w3.org/WAI/mobile/)

---

**작성일**: 2025-01-11  
**작성자**: AI Assistant  
**버전**: 1.1 (모바일 웹 환경 고려사항 추가)
