# 채팅 무한 루프 및 전환 오류 해결

## 문제 요약

채팅 기능 구현 및 최적화 과정에서 다음과 같은 오류들이 발생했습니다:

1. **Maximum update depth exceeded** - 무한 루프 오류
2. **getSnapshot 캐싱 문제** - Zustand selector 최적화 이슈
3. **빠른 채팅방 전환 시 오류** - 비동기 작업 충돌

---

## 1. Maximum update depth exceeded 오류 해결

### 문제 증상
```
Maximum update depth exceeded. This can happen when a component calls setState inside useEffect, but useEffect either doesn't have a dependency array, or one of the dependencies changes on every render.
```

### 원인 분석

#### 1.1 ChatRoomCard.tsx의 useEffect 무한 루프
- **위치**: `BEBEE-FE/src/domain/chat/components/ChatRoomCard.tsx`
- **문제**: `postDetail?.postId` 체크가 의존성 배열에 없어서 무한 루프 발생
- **원인**: `postApi.getPostDetail`이 호출되면 `setPostDetail`이 실행되고, 이것이 다시 useEffect를 트리거

#### 1.2 MessageList.tsx의 Zustand selector 문제
- **위치**: `BEBEE-FE/src/domain/chat/components/MeessageList.tsx`
- **문제**: `useChatStore((state) => state.getMessages(chatroomId))`가 매번 새로운 배열 참조를 반환
- **원인**: `getMessages` 함수가 호출될 때마다 새 배열을 반환하여 useEffect가 계속 실행됨

### 해결 방법

#### ChatRoomCard.tsx 수정
```typescript
// 수정 전
useEffect(() => {
  const postIdToUse = currentPost?.postId || activeRoom?.postId;
  if (!postIdToUse) return;
  if (postDetail?.postId === postIdToUse) return;
  // ...
}, [currentPost?.postId, activeRoom?.postId]); // postDetail이 의존성에 없음

// 수정 후
useEffect(() => {
  const postIdToUse = currentPost?.postId || activeRoom?.postId;
  if (!postIdToUse) return;
  if (postDetail?.postId === postIdToUse && !isLoadingPost) return; // 로딩 중 체크 추가
  if (isLoadingPost) return; // 중복 요청 방지
  
  // ...
}, [currentPost?.postId, activeRoom?.postId, postDetail?.postId]); // postDetail?.postId 추가
```

**주요 변경사항:**
- `postDetail?.postId`를 의존성 배열에 추가하여 이미 로드된 데이터는 재요청하지 않도록 함
- `isLoadingPost` 체크를 추가하여 중복 요청 방지

#### MessageList.tsx 수정
```typescript
// 수정 전
const messages = useChatStore((state) => state.getMessages(chatroomId)); // 매번 새 배열 반환

// 수정 후
const rawMessages = useChatStore((state) =>
  chatroomId ? state.messagesByChatroom[chatroomId]?.messages || [] : []
); // 직접 배열 참조 접근

const messages = useMemo(() => {
  if (!rawMessages || rawMessages.length === 0) return [];
  return [...rawMessages].sort(
    (a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime()
  );
}, [rawMessages]); // useMemo로 정렬 결과 캐싱
```

**주요 변경사항:**
- `getMessages` 함수 대신 직접 `messagesByChatroom[chatroomId]?.messages`에 접근
- `useMemo`를 사용하여 정렬 결과를 캐싱하여 불필요한 재계산 방지

---

## 2. getSnapshot 캐싱 문제 해결

### 문제 증상
```
The result of getSnapshot should be cached to avoid an infinite loop
```

### 원인 분석
- Zustand의 `useChatStore` selector에서 매번 새로운 배열을 반환
- React의 `useSyncExternalStore`가 이를 변경으로 감지하여 무한 루프 발생

### 해결 방법

**Zustand selector 최적화:**
```typescript
// 함수 호출 방식 (문제)
const messages = useChatStore((state) => state.getMessages(chatroomId));

// 직접 접근 방식 (해결)
const rawMessages = useChatStore((state) =>
  chatroomId ? state.messagesByChatroom[chatroomId]?.messages || [] : []
);
```

**useMemo를 통한 추가 최적화:**
```typescript
const messages = useMemo(() => {
  if (!rawMessages || rawMessages.length === 0) return [];
  return [...rawMessages].sort(
    (a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime()
  );
}, [rawMessages]);
```

**핵심 원리:**
- Zustand는 기본적으로 shallow equality를 사용하여 배열 참조가 변경되지 않으면 리렌더링하지 않음
- 함수를 호출하면 매번 새 배열이 생성되지만, 직접 접근하면 동일한 참조를 유지
- `useMemo`로 정렬 결과를 캐싱하여 불필요한 재계산 방지

---

## 3. 메시지 순서 정렬

### 문제
- 메시지가 시간 순서대로 정렬되지 않음
- 최신 메시지가 아래에 오지 않음

### 해결 방법

```typescript
const messages = useMemo(() => {
  if (!rawMessages || rawMessages.length === 0) return [];
  // createdAt 기준 오름차순 정렬 (오래된 메시지가 위, 최신 메시지가 아래)
  return [...rawMessages].sort(
    (a, b) => new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime()
  );
}, [rawMessages]);
```

**정렬 기준:**
- `createdAt` 필드를 기준으로 오름차순 정렬
- 오래된 메시지가 위에, 최신 메시지가 아래에 표시

---

## 4. 빠른 채팅방 전환 시 오류 해결

### 문제 증상
- 채팅방을 빠르게 입장했다가 나갔다가 다른 채팅방을 입장하면 오류 발생
- 이전 채팅방의 비동기 응답이 새 채팅방에 적용됨
- 메시지가 섞여서 표시됨

### 원인 분석

1. **비동기 작업 취소 없음**: 이전 채팅방의 API 요청이 완료되기 전에 새 채팅방으로 전환
2. **상태 검증 부재**: 응답이 현재 채팅방에 해당하는지 확인하지 않음
3. **중복 요청**: 여러 요청이 동시에 실행될 수 있음

### 해결 방법

#### 4.1 ChatRoomPage.tsx 수정

```typescript
const ChatRoom = () => {
  const { chatroomId } = useParams<{ chatroomId: string }>();
  const currentChatroomIdRef = useRef<string | undefined>(chatroomId);
  const isMountedRef = useRef(true);

  // 1. 채팅방 정보 조회 및 소켓 연결
  useEffect(() => {
    if (!chatroomId) return;
    
    currentChatroomIdRef.current = chatroomId;
    isMountedRef.current = true;

    const initializeChatRoom = async () => {
      try {
        if (!activeRoom || activeRoom.chatroomId !== chatroomId) {
          const roomData = await chatApi.openChatRoom(undefined, chatroomId);
          
          // 요청 완료 후 chatroomId가 변경되었는지 확인
          if (currentChatroomIdRef.current !== chatroomId || !isMountedRef.current) {
            console.log("채팅방이 변경되어 응답 무시:", chatroomId);
            return;
          }
          
          setActiveRoom(roomData);
        }
        connect();
      } catch (error) {
        console.error("채팅방 초기화 실패:", error);
      }
    };

    initializeChatRoom();

    return () => {
      isMountedRef.current = false;
    };
  }, [chatroomId]);

  // 2. 메시지 조회
  useEffect(() => {
    if (!chatroomId) return;
    if (!activeRoom || activeRoom.chatroomId !== chatroomId) return;

    const targetChatroomId = chatroomId;
    let isCancelled = false;

    const loadMessages = async () => {
      try {
        await fetchHistory(targetChatroomId, true);
        
        // 요청 완료 후 chatroomId가 변경되었는지 확인
        if (currentChatroomIdRef.current !== targetChatroomId || isCancelled) {
          console.log("채팅방이 변경되어 메시지 로딩 취소:", targetChatroomId);
          return;
        }
      } catch (error) {
        if (!isCancelled) {
          console.error("메시지 로딩 실패:", error);
        }
      }
    };

    loadMessages();

    return () => {
      isCancelled = true;
    };
  }, [chatroomId, activeRoom?.chatroomId]);
};
```

**주요 변경사항:**
- `useRef`를 사용하여 현재 유효한 `chatroomId` 추적
- 비동기 작업 완료 후 `chatroomId` 변경 여부 확인
- 변경된 경우 응답 무시
- `isCancelled` 플래그로 cleanup 처리

#### 4.2 useChatStore.ts 수정

```typescript
fetchHistory: async (chatroomId: string, isFirstLoad = false) => {
  // 로딩 중이면 중복 요청 방지
  if (get().isLoadingHistory) {
    console.log("이미 메시지 로딩 중, 요청 스킵");
    return;
  }

  try {
    set({ isLoadingHistory: true });
    const data = await chatApi.getMessages(chatroomId, cursor);

    // 요청 완료 후 chatroomId가 여전히 유효한지 확인
    const currentState = get();
    if (currentState.activeRoom?.chatroomId !== chatroomId && isFirstLoad) {
      console.log("채팅방이 변경되어 메시지 응답 무시:", chatroomId);
      set({ isLoadingHistory: false });
      return;
    }

    set((state) => ({
      isLoadingHistory: false,
      messagesByChatroom: {
        ...state.messagesByChatroom,
        [chatroomId]: {
          messages: isFirstLoad ? data.messages : [...data.messages, ...prevRoomData.messages],
          hasNext: data.hasNext,
          lastChatId: data.nextChatId,
        },
      },
    }));
  } catch (error) {
    set({ isLoadingHistory: false });
    console.error(`${chatroomId} 내역 로드 실패:`, error);
  }
},
```

**주요 변경사항:**
- `isLoadingHistory` 플래그로 중복 요청 방지
- 첫 로딩 시 `activeRoom.chatroomId`와 비교하여 잘못된 응답 무시

#### 4.3 MessageList.tsx 수정

```typescript
useEffect(() => {
  if (!chatroomId || !scrollRef.current) return;

  const container = scrollRef.current;
  const currentChatroomId = chatroomId; // 현재 chatroomId 캡처

  const handleScroll = () => {
    if (isLoadingMoreRef.current) return;
    
    // chatroomId가 변경되었는지 확인
    if (currentChatroomId !== chatroomId) {
      return;
    }

    if (isAtTop && hasNext) {
      isLoadingMoreRef.current = true;
      
      fetchHistory(currentChatroomId, false).then(() => {
        // chatroomId가 변경되었는지 다시 확인
        if (currentChatroomId !== chatroomId || !scrollRef.current) {
          isLoadingMoreRef.current = false;
          return;
        }
        
        requestAnimationFrame(() => {
          if (container && scrollRef.current && currentChatroomId === chatroomId) {
            // 스크롤 위치 조정
          }
          isLoadingMoreRef.current = false;
        });
      }).catch(() => {
        isLoadingMoreRef.current = false;
      });
    }
  };

  container.addEventListener("scroll", handleScroll);
  return () => {
    container.removeEventListener("scroll", handleScroll);
    isLoadingMoreRef.current = false;
  };
}, [chatroomId, hasNext, fetchHistory]);
```

**주요 변경사항:**
- 스크롤 핸들러에서 `chatroomId` 변경 확인
- `fetchHistory` 완료 후 `chatroomId` 재확인
- 변경된 경우 스크롤 위치 조정 스킵

---

## 해결 결과

✅ **무한 루프 오류 해결**
- `ChatRoomCard`의 `useEffect` 의존성 최적화
- `MessageList`의 Zustand selector 최적화

✅ **getSnapshot 캐싱 문제 해결**
- 직접 배열 참조 접근으로 변경
- `useMemo`를 통한 정렬 결과 캐싱

✅ **메시지 순서 정렬**
- `createdAt` 기준 오름차순 정렬
- 최신 메시지가 아래에 표시

✅ **빠른 채팅방 전환 시 오류 해결**
- 비동기 작업 취소 로직 추가
- 응답 검증 로직 추가
- 중복 요청 방지

---

## 핵심 원칙

1. **의존성 배열 정확성**: useEffect의 의존성 배열에 실제로 사용하는 모든 값 포함
2. **Zustand selector 최적화**: 함수 호출 대신 직접 참조 접근
3. **비동기 작업 취소**: 컴포넌트 언마운트 또는 상태 변경 시 비동기 작업 취소
4. **응답 검증**: 비동기 작업 완료 후 현재 상태와 일치하는지 확인
5. **중복 요청 방지**: 로딩 플래그를 사용하여 중복 요청 방지

---

## 참고 파일

- `BEBEE-FE/src/domain/chat/components/ChatRoomCard.tsx`
- `BEBEE-FE/src/domain/chat/components/MeessageList.tsx`
- `BEBEE-FE/src/domain/chat/pages/ChatRoomPage.tsx`
- `BEBEE-FE/src/domain/chat/store/useChatStore.ts`
