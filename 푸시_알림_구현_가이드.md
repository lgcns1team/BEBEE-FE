# 푸시 알림 구현 가이드

## 목차

1. [개요](#개요)
2. [구현된 기능](#구현된-기능)
3. [파일 구조](#파일-구조)
4. [동작 흐름](#동작-흐름)
5. [컴포넌트 설명](#컴포넌트-설명)
6. [사용 방법](#사용-방법)
7. [테스트 방법](#테스트-방법)

---

## 개요

이 프로젝트는 Firebase Cloud Messaging (FCM)을 사용하여 웹 푸시 알림을 구현했습니다.

### 주요 기능

- ✅ FCM 토큰 발급 및 서버 등록
- ✅ 포그라운드 메시지 수신 및 표시
- ✅ 백그라운드 메시지 수신 및 알림 표시
- ✅ 알림 클릭 시 앱 열기
- ✅ 모바일/PC 기기 자동 감지

---

## 구현된 기능

### 1. FCM 토큰 관리

- 사용자 로그인 시 자동으로 FCM 토큰 발급
- 서버에 토큰 등록 (`/notification/notifications/fcm/tokens`)
- 모바일/PC 기기 자동 감지하여 `deviceType` 설정
- **인증 헤더 자동 포함**: `axiosInstance` 인터셉터를 통해 `Authorization: Bearer {accessToken}` 헤더 자동 추가

### 2. 메시지 수신 처리

- **포그라운드**: 페이지가 열려있을 때 메시지 수신
- **백그라운드**: 페이지가 닫혀있을 때 Service Worker가 알림 표시

### 3. 메시지 표시

- **모달 팝업**: 포그라운드 메시지 수신 시 모달로 표시
- **브라우저 알림**: 백그라운드 메시지 수신 시 시스템 알림 표시

---

## 파일 구조

```
src/
├── hooks/
│   └── useFirebaseHandler.ts      # FCM 초기화 및 메시지 리스너
├── store/
│   └── useFCMStore.ts             # FCM 메시지 상태 관리
├── components/
│   └── FCMMessageModal.tsx        # 푸시 알림 모달 컴포넌트
├── api/
│   └── notificationApi.ts         # FCM 토큰 등록 API
└── domain/post/pages/
    └── HomePage.tsx                # FCM 초기화 진입점

public/
└── firebase-messaging-sw.js       # Service Worker (백그라운드 처리)
```

---

## 동작 흐름

### 전체 흐름

```
사용자 로그인
    ↓
/home 페이지 접속
    ↓
FCM 초기화 (initializeFCM)
    ├─ Service Worker 등록
    ├─ 알림 권한 요청
    └─ FCM 토큰 획득
    ↓
서버에 토큰 등록
    ↓
메시지 수신 준비 완료
```

### 포그라운드 메시지 수신

```
FCM 서버 → 메시지 전송
    ↓
onMessage() 콜백 실행
    ↓
useFCMMessageStore.showMessage() 호출
    ↓
FCMMessageModal 표시 (상단)
    ↓
사용자가 X 버튼 클릭 또는 ESC 키
    ↓
모달 닫기
```

### 백그라운드 메시지 수신

```
FCM 서버 → 메시지 전송
    ↓
Service Worker의 onBackgroundMessage() 실행
    ↓
브라우저 알림 표시
    ↓
사용자가 알림 클릭
    ↓
앱 열기 (또는 포커스)
    ↓
메인 스레드로 메시지 전달
    ↓
FCMMessageModal 표시
```

---

## 컴포넌트 설명

### 1. `FCMMessageModal.tsx`

**역할**: 푸시 알림 메시지를 상단 모달로 표시

**주요 기능**:

- 상단에 고정된 모달 (top: 0)
- 메시지 제목 및 본문 표시
- X 버튼으로 닫기
- ESC 키로 닫기
- 슬라이드 다운 애니메이션

**사용 위치**: `App.tsx` (전역)

**스타일 특징**:

- 오버레이 없음 (배경 어둡게 하지 않음)
- 상단 중앙에 위치
- 최대 너비 400px
- 흰색 배경, 둥근 모서리, 그림자 효과

**코드 예시**:

```typescript
import { FCMMessageModal } from "./components/FCMMessageModal";

function App() {
  return (
    <ThemeProvider theme={theme}>
      <GlobalStyle />
      <FCMMessageModal /> {/* 전역 모달 */}
      <BrowserRouter>{/* ... */}</BrowserRouter>
    </ThemeProvider>
  );
}
```

**Props**: 없음 (Zustand store에서 상태 관리)

**상태 관리**:

```typescript
const { message, isOpen, closeMessage } = useFCMMessageStore();
```

### 2. `useFCMStore.ts`

**역할**: FCM 메시지 상태 관리

**상태**:

- `message`: 현재 메시지 (제목, 본문, 데이터)
- `isOpen`: 모달 열림/닫힘 상태

**함수**:

- `showMessage(payload)`: 메시지 표시
- `closeMessage()`: 메시지 닫기

**코드 예시**:

```typescript
interface FCMMessageStore {
  message: FCMMessage | null;
  isOpen: boolean;
  showMessage: (payload: MessagePayload) => void;
  closeMessage: () => void;
}
```

### 3. `useFirebaseHandler.ts`

**역할**: FCM 초기화 및 메시지 리스너 설정

**주요 함수**:

#### `initializeFCM()`

- FCM 초기화 및 토큰 획득
- Service Worker 등록
- 알림 권한 요청

#### `setupFCMMessageListener()`

- 포그라운드 메시지 리스너 설정
- 메시지 수신 시 `useFCMMessageStore.showMessage()` 호출

#### `setupServiceWorkerMessageListener()`

- Service Worker로부터 메시지 수신 리스너 설정
- 백그라운드 메시지 및 알림 클릭 이벤트 처리

### 4. `notificationApi.ts`

**역할**: FCM 토큰 서버 등록 API

**주요 기능**:

- FCM 토큰을 서버에 등록
- `deviceType`과 함께 전송 (`WEB_PC`, `WEB_MOBILE`, `IOS`, `ANDROID`)
- **자동 인증**: `axiosInstance` 인터셉터를 통해 Authorization 헤더 자동 포함

**요청 형식**:

```typescript
POST /notification/notifications/fcm/tokens
Headers:
  Authorization: Bearer {accessToken}  // 자동 포함
  X-Member-Id: {memberId}              // 자동 포함
Body:
{
  "token": "FCM_TOKEN_HERE",
  "deviceType": "WEB_PC" | "WEB_MOBILE" | "IOS" | "ANDROID"
}
```

### 5. `firebase-messaging-sw.js`

**역할**: Service Worker에서 백그라운드 메시지 처리

**주요 기능**:

- 백그라운드 메시지 수신 (`onBackgroundMessage`)
- 브라우저 알림 표시
- 알림 클릭 이벤트 처리
- 메인 스레드로 메시지 전달

---

## 사용 방법

### 1. FCM 초기화 (자동)

사용자가 로그인하고 `/home` 페이지에 접속하면 자동으로 FCM이 초기화됩니다:

```typescript
// HomePage.tsx
useEffect(() => {
  if (!user || fcmInitialized.current) return;
  if (location.pathname !== "/home") return;

  const setupFCM = async () => {
    const token = await initializeFCM();
    if (token) {
      const deviceType = detectDeviceType();
      await registerFCMToken(token, deviceType);
      fcmInitialized.current = true;
    }
    setupFCMMessageListener();
  };

  setupFCM();
}, [user, location.pathname]);
```

### 2. 메시지 수신 (자동)

메시지가 수신되면 자동으로 처리됩니다:

- **포그라운드**: `setupFCMMessageListener()`가 자동으로 메시지를 받아 모달 표시
- **백그라운드**: Service Worker가 자동으로 알림 표시

### 3. 메시지 표시 (자동)

`FCMMessageModal`이 `App.tsx`에 포함되어 있어 자동으로 표시됩니다.

---

## 테스트 방법

### 1. FCM 토큰 발급 확인

1. 브라우저 개발자 도구 열기
2. `/home` 페이지 접속
3. 콘솔에서 다음 메시지 확인:
   ```
   🔔 [FCM] 초기화 시작...
   📱 [디바이스 감지] { userAgent: "...", detectedType: "WEB_PC" | "WEB_MOBILE", isMobile: false }
   ✅ [FCM] 토큰 획득 성공: dX3kL9mN2Pq:APA91bH_...
   📤 [FCM] 토큰 서버 등록 요청: { url: "...", body: { token: "...", deviceType: "WEB_PC" } }
   ✅ [FCM] 토큰 서버 등록 응답: { success: true }
   ✅ [FCM] 토큰 서버 등록 성공
   ```

**주요 로그 설명**:

- `📱 [디바이스 감지]`: 현재 기기 타입 감지 결과
- `📤 [FCM] 토큰 서버 등록 요청`: 서버로 전송되는 요청 데이터 확인
- `✅ [FCM] 토큰 서버 등록 응답`: 서버 응답 확인

### 2. 포그라운드 메시지 테스트

**Firebase Console에서 테스트 메시지 전송**:

1. Firebase Console → Cloud Messaging → 새 알림 작성
2. 알림 제목 및 본문 입력
3. "테스트 메시지 전송" 클릭
4. FCM 토큰 입력
5. 전송

**예상 결과**:

- 페이지가 열려있으면 **상단에 모달 팝업** 표시
- X 버튼 또는 ESC 키로 닫기 가능
- 브라우저 알림도 함께 표시 (선택사항)

### 2-1. 모바일 모드 테스트 (개발자 도구)

**PC에서 개발자 도구 모바일 모드로 테스트**:

1. 브라우저 개발자 도구 열기
2. 디바이스 툴바 활성화 (모바일 모드)
3. iPhone/iPad/Android 선택
4. `/home` 페이지 접속
5. 콘솔에서 다음 로그 확인:
   ```
   📱 [디바이스 감지] { detectedType: "WEB_MOBILE", isMobile: true }
   📤 [FCM] 토큰 서버 등록 요청: { body: { deviceType: "WEB_MOBILE" } }
   ```

**예상 결과**:

- `deviceType: "WEB_MOBILE"`로 서버에 전송됨
- 실제 모바일 기기와 동일한 방식으로 동작

### 3. 백그라운드 메시지 테스트

1. 페이지를 닫거나 다른 탭으로 이동
2. Firebase Console에서 메시지 전송
3. 브라우저 알림 확인
4. 알림 클릭
5. 앱이 열리고 **상단에 모달 팝업** 표시

### 4. 서버에서 메시지 전송

서버에서 FCM Admin SDK를 사용하여 메시지 전송:

```javascript
// Node.js 예시
const admin = require("firebase-admin");

const message = {
  notification: {
    title: "새 메시지",
    body: "푸시 알림 테스트입니다.",
  },
  token: "FCM_TOKEN_HERE", // 사용자의 FCM 토큰
};

admin
  .messaging()
  .send(message)
  .then((response) => {
    console.log("Successfully sent message:", response);
  })
  .catch((error) => {
    console.log("Error sending message:", error);
  });
```

---

## API 요청/응답 형식

### FCM 토큰 등록 API

**엔드포인트**: `POST /notification/notifications/fcm/tokens`

**요청 헤더** (자동 포함):

```
Authorization: Bearer {accessToken}
X-Member-Id: {memberId}
Content-Type: application/json
```

**요청 Body**:

```json
{
  "token": "dX3kL9mN2Pq:APA91bH_1234567890abcdefghijklmnopqrstuvwxyz",
  "deviceType": "WEB_PC"
}
```

**deviceType 옵션**:

- `WEB_PC`: 데스크톱/PC 브라우저
- `WEB_MOBILE`: 모바일 브라우저 (개발자 도구 모바일 모드 포함)
- `IOS`: iOS 앱
- `ANDROID`: Android 앱

**응답**:

```json
{
  "success": true,
  "message": "FCM 토큰이 성공적으로 등록되었습니다."
}
```

**참고**:

- `axiosInstance`의 요청 인터셉터가 자동으로 `Authorization`과 `X-Member-Id` 헤더를 추가합니다.
- 개발 환경에서는 콘솔에 `📤 [FCM] 토큰 서버 등록 요청` 로그로 실제 전송 데이터를 확인할 수 있습니다.

---

## 메시지 페이로드 형식

### 표준 알림 메시지

```json
{
  "notification": {
    "title": "알림 제목",
    "body": "알림 본문",
    "icon": "/icon.png"
  },
  "data": {
    "customKey": "customValue"
  }
}
```

### 데이터만 포함된 메시지

```json
{
  "data": {
    "title": "데이터 제목",
    "body": "데이터 본문",
    "customKey": "customValue"
  }
}
```

---

## 주의사항

### 1. 알림 권한

- 사용자가 알림 권한을 거부하면 FCM 토큰을 획득할 수 없습니다
- 권한이 거부된 경우 브라우저 설정에서 수동으로 허용해야 합니다

### 2. HTTPS 필수

- 프로덕션 환경에서는 HTTPS가 필수입니다
- `localhost`는 개발 환경에서 허용됩니다

### 3. Service Worker

- Service Worker는 `/` 범위로 등록됩니다
- 파일은 `public/firebase-messaging-sw.js`에 위치해야 합니다

### 4. 모바일 환경

- Android: 완전 지원
- iOS Safari (16.4+): 지원 (일부 제한사항)
- iOS Safari (16.3 이하): 미지원

### 5. 브라우저 호환성

- Chrome, Firefox, Edge: 완전 지원
- Safari (macOS): 지원
- Safari (iOS 16.4+): 지원

---

## 트러블슈팅

### 1. 모달이 표시되지 않는 경우

**확인 사항**:

- `FCMMessageModal`이 `App.tsx`에 포함되어 있는지 확인
- `useFCMMessageStore`의 `isOpen` 상태 확인
- 브라우저 콘솔에서 오류 확인

### 2. 백그라운드 알림이 표시되지 않는 경우

**확인 사항**:

- Service Worker가 등록되어 있는지 확인 (Application → Service Workers)
- 알림 권한이 허용되어 있는지 확인
- `firebase-messaging-sw.js` 파일이 올바른 위치에 있는지 확인

### 3. 메시지가 수신되지 않는 경우

**확인 사항**:

- FCM 토큰이 서버에 등록되어 있는지 확인
- 서버에서 올바른 FCM 토큰으로 메시지를 전송하는지 확인
- Firebase Console에서 테스트 메시지 전송 시도
- 콘솔 로그 확인: `📤 [FCM] 토큰 서버 등록 요청`으로 실제 전송 데이터 확인

### 4. 토큰 등록이 실패하는 경우

**확인 사항**:

- 인증 토큰이 유효한지 확인 (`Authorization` 헤더 자동 포함됨)
- 서버 API 엔드포인트 확인: `/notification/notifications/fcm/tokens`
- 요청 body 형식 확인: `{ token: string, deviceType: "WEB_PC" | "WEB_MOBILE" }`
- 개발자 도구 Network 탭에서 실제 요청/응답 확인

---

## 참고 자료

- [FCM 토큰 발급 및 서버 전송 과정](./FCM_토큰_발급_및_서버_전송_과정.md)
- [FCM 토큰 등록 기능](./FCM_토큰_등록_기능.md)
- [Firebase Cloud Messaging 문서](https://firebase.google.com/docs/cloud-messaging)
- [Web Push Notifications 가이드](https://web.dev/push-notifications-overview/)
